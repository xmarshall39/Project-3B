using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NearestCorner : MonoBehaviour
{

    //Creates new chasers at random points off screen
    //at regular inervals

    //Create new turrets at random intervals and
    //transform them to a corner of the screen

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
